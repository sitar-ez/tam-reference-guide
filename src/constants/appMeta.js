import packageJson from "../../package.json";

/**
 * Application metadata surfaced in the UI and print/export output.
 */
export const APP_NAME = "Tam Integration — Guide to Popular Molecules";
export const APP_VERSION = packageJson.version;
